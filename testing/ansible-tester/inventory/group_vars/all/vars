---
canonical_hostname: dstesting_data.dstesting_default

check_routes_timeout: 5

irods_version: 4.1.10
irods_zone_name: testing

tls_key: "{{ tls_key_default }}"
tls_cert: "{{ tls_cert_default }}"
tls_intermediate_certs: "{{ tls_root_cert }}"
